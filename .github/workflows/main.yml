name: Update Petshop
on:
  push:
    branches:
    - main
    paths:
    - .github/workflows/main.yml
jobs:
  refresh:
    runs-on: ubuntu-latest    
    steps:
      - name: checkout code
        uses: actions/checkout@v3
        with:
          path: local
      - name: run generator
        run: docker run -v localvol:${GITHUB_WORKSPACE}/local openapitools/openapi-generator-cli generate -i /localvol/petstore.yaml -g aspnetcore -o /localvol/out/aspnetcore --additional-properties=aspnetCoreVersion=6.0
